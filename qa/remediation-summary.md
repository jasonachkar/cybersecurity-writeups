# Remediation summary — PR #5 (`codex/validated-gh-pages-deployment`)

This file records the follow-up remediation against the acceptance criteria.
Executable results are regenerated by CI and stamped into `qa/*.json`.

## Phase 1 — AI approval replay race

| Item | Resolution |
|------|------------|
| Non-atomic check-then-set | Replaced with `ApprovalStore` (`getApprovedAction` + atomic `consumeIfUnused`) |
| Concurrent replay | Test forces overlapping reads via `readDelayMs`; exactly one executor invocation; loser gets `APPROVAL_REPLAYED` |
| Docs | Homepage card, site-config evidence, and AI article bound the claim to local in-memory CAS |
| Sentence casing | “System prompts and content filters are not authorization boundaries.” |

**Tests:** `node --test labs/ai-agent-security/tests/broker.test.js` (12 cases including concurrent replay).

**Limitation:** In-memory CAS models durable conditional write; not Redis/PostgreSQL/DynamoDB.

## Phase 2 — Legacy utilities

| Artifact | Outcome |
|----------|---------|
| `threat-intel/scripts/cloudtrail/` | **Fixed** — accurate CT fields, no `time.Now()` fallback, MFA/success checks, chronological chains, thresholded KMS denials, admin-event wording + tests |
| `cloud-security/scripts/k8s-rbac/` | **Fixed** — subresource-accurate heuristic (CSR approval, SA token, TokenReview oracle, impersonate, bind/escalate, etc.) + tests |
| `appsec/scripts/oauth-pkce/` | **Fixed** — library + `go test` (was checks-in-main) |
| `jwt-validator.go` | **Retired** — Node OAuth/OIDC lab is canonical; no `exp == now` utility remains |
| `idor-scanner.go` | **Retired** |
| `iam-analyzer.go` | **Retired** |
| `scp-auditor.go` | **Retired** |
| `pipeline-gate.go` | **Retired** |
| `sbom-diff.go` | **Retired** |
| `secret-scanner.go` | **Retired** (Gitleaks is authoritative) |
| Azure Firewall `main.tf` | **Retired** — open RDP `*` default removed; archive notice updated |

## Phase 3 — Accessibility

| Item | Resolution |
|------|------------|
| Dark footer contrast | Explicit `#e2e8f0` on dark footer backgrounds |
| Dual palette/viewport | `scripts/run-accessibility-audit.mjs` audits default+slate × desktop+mobile |
| landmark-unique | `assets/javascripts/portfolio-a11y.js` + audit-time labeling of `.md-code__nav` |

## Phase 4–7 — QA provenance, verify:all, CI, pins

| Item | Resolution |
|------|------------|
| Commit-bound reports | `scripts/qa-provenance.mjs` stamps `executedAt`, `gitCommit`, `gitTree`, tools, result |
| Report verification | `scripts/verify-qa-provenance.mjs` |
| `npm run verify:all` | Orchestrates static, labs, go, terraform, a11y, links, secrets, summary, provenance |
| CI | `.github/workflows/gh-pages-quality.yml` — required jobs + summary gate; artifacts uploaded; no deploy |
| Playwright | Pinned exact `1.61.1` |
| Policy Python deps | `scripts/requirements-policy.txt` |

## Remaining intentional limitations

- Kyverno image policy: schema/offline only (not live signature admission).
- Tetragon: schema validation only (no live cluster enforcement).
- Supply-chain: offline adapter contract only (no production DSSE verification).
- AI ApprovalStore: teaching in-memory CAS.
- No cloud resources deployed during validation.
- Uber newsroom external link may remain bot-blocked (recorded as limited, not broken).

## PR state

PR #5 remains **draft** until the latest head commit shows all required workflow jobs green.
